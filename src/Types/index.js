import { KEYS } from "./keys";
import { PERFORMANCE_TIER } from "./performance";
import { URL_PARAMS } from "./params";
import { COMPONENTS } from "./components";
import { ACTIONS } from "./actions";

export const TYPES = {
  ...KEYS,
  ...PERFORMANCE_TIER,
  ...URL_PARAMS,
  ...COMPONENTS,
  ...ACTIONS,
};
